// Ainto Release build configuration
// For distribution, set CODE_SIGN_IDENTITY to your Developer ID

#include "base.xcconfig"

// For local release builds without Developer ID, use the self-signed certificate
// For distribution, replace with: Developer ID Application: Your Name (TEAM_ID)
CODE_SIGN_IDENTITY = Local Self-Signed
CODE_SIGN_STYLE = Manual
DEVELOPMENT_TEAM =
OTHER_CODE_SIGN_FLAGS = --timestamp=none
SWIFT_COMPILATION_MODE = wholemodule
SWIFT_OPTIMIZATION_LEVEL = -O
